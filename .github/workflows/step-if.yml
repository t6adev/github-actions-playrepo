name: Play step-if
on:
  push:
    branches:
      - test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo 'hello'
    - run: echo 'exit' && exit 1
    - name: at if()
      if: ${{ failure() }}
      run: echo 'run this'
